
/* gamma(P, x, y) := (P And x) Or (Not P And y); */

55 # LogicSimplify(gamma(_P,_x,_y)> _n) <--
       LogicSimplify(P And (x>n)) Or ( (Not p) And (y>n));


- Use p^2-1 = 24*n
- Use the fact that the above n, (n-1) cannot be divisible by 3.
- Start with least significant digit.
- Use the above to walk faster through n.
- Use the above to truncate
  multiplication routes (24*n = ...xyz, being an impossibility
  for any n, so truncate).


