[title:Continued fraction expansion 2]

 

The variable [link:contfrac:contfrac;]now contains the continued fraction expansion.

 

Convert it back to a [link:numeric form:N(contfrac Where rest == 0);].

 

Compare with the [link:number it is approximating:N(Exp(1));].

 

[link:Back to previous menu:approx.yml]
